spring:
  kafka:
    consumer:
      auto-offset-reset: earliest

#Observability
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

security:
  basic:
    enabled: false

